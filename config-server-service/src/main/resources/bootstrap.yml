server:
  port: 8888
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: $SPRING_CLOUD_CONFIG_SERVER_GIT_URI
      discovery:
        enabled: true
        
         
        